	<h3> <%= t('.title')%> </h3>
	<table class="table table-striped">
		<%= render (@cart.line_items)%>
		<tr>
			<td colspan="3">Go Jek shipping cost</td>
			<td>Rp 15.000</td>
		</tr>
		<tr class="total_line">
			<td colspan="3"> Total </td>
			<td class="total_cell">Rp <%= number_with_delimiter(@cart.total_price_with_go_jek_cost) %> </td>		
		</tr>
		
	</table>
	<%= button_to t('.checkout'), new_order_path, method: :get, class: "btn btn-danger" %><hr/>
	<%= button_to t('.empty'), @cart, method: :delete, data: { confim: "Are you sure?" } %>
